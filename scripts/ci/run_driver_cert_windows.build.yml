---
platform: windows

inputs:
  - name: smb-volume-release
  - name: persi-ci

params:
  username:
  password:
  host:

run:
  path: powershell
  args:
    - "-ExecutionPolicy"
    - "ByPass"
    - "-File"
    - "smb-volume-release/scripts/ci/run_driver_cert.ps1"
